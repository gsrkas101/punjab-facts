{
  "name": "punjab-facts",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Punjab Facts — a nonpartisan, government-data-only view of the state of Punjab, India.",
  "license": "ISC",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "observable deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.4",
    "yaml": "^2.9.0"
  }
}
