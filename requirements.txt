# Python ETL (Stage A) dependencies.
pandas>=2.0
pyyaml>=6.0
requests>=2.31
openpyxl>=3.1        # read .xlsx sources (RBI handbook, UDISE+, etc.)
# datagovindia>=0.2  # data.gov.in API wrapper — enable when wiring live API pulls
